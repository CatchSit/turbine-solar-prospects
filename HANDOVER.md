# Turbine Energy — Solar Prospect Map — Handover

## 1. Project Overview

A prospecting tool for **Turbine Energy**, a UK commercial solar installer. It visualises commercial/industrial buildings in a pilot region (Yorkshire & Humber) that plausibly have moderate-to-high electricity usage and do **not** already have rooftop solar, so the sales team has a warm lead list instead of cold-calling blind.

This is a sibling project to `mcs-map` (Amco Renewables' installer map/CRM at `C:\Users\GregRoy\mcs-map`) — it reuses the same architecture philosophy (static HTML, Supabase backend, GitHub Pages hosting, no build tool) but is a **separate client, separate repo, separate Supabase project**.

**Repo:** https://github.com/CatchSit/turbine-solar-prospects — **public** (made public 2026-08-12 so GitHub Pages could run on the free plan, matching `mcs-map`'s own setup; no secrets are in the source, the anon key is meant to be public, and RLS + login is the real gate)
**Supabase project:** `turbine-solar-prospects` (created — confirm migrations have been run before assuming the schema exists, see Section 4)
**GitHub Pages:** **live** at `https://catchsit.github.io/turbine-solar-prospects/`, enabled 2026-08-12. Microsoft/Azure AD sign-in is now **fully working end-to-end** — verified 2026-08-17 with a real `@turbineenergyuk.co.uk` account completing the OAuth login live (Section 1, item 2). **Still not yet rolled out or announced to Turbine Energy staff** — the map itself will appear empty to anyone who signs in, since solar enrichment hasn't run yet (Section 1, item 1). Don't announce the URL until that's resolved too.
**Local folder:** `C:\Users\GregRoy\Projects\turbine-solar-prospects` — already renamed from the original `commercial-map` scaffold name.

**Status: v1 pilot build, real data loaded, two external dependencies pending.** Data pipeline code, the map frontend, Turbine Energy's real branding, and an Azure AD login gate are all built and pushed. The `prospects` table holds **37,134 real Yorkshire & Humber buildings** (**22,016 `source: 'epc'`** from the full 2011–2026 non-domestic EPC bulk export, plus **15,118 `source: 'voa'`** seeded directly from VOA business-rates data for buildings with no EPC certificate at all — Section 4, Step 6); **35,141 of those (~95%) are geocoded** (lat/lng filled via postcodes.io). `index.html` queries Supabase live (not a static file) and shows "Failed to load prospect data" only if that live query fails (see Section 6 — this has genuinely happened live multiple times on 2026-08-21 while iterating on load performance at this new, much larger row count; the current fetch is deliberately back to a plain sequential loop after concurrent variants repeatedly caused live `57014` statement-timeout failures, see Section 6). **CRM contact-logging layer is now built** (2026-08-17, migration `007`) — reps log contacts via a modal on the map, a manager-only `dashboard.html` gives activity charts/table/CSV export, matching mcs-map's map+CRM+dashboard shape but with a manager-only read policy where mcs-map allows any authenticated read (Section 5). Authentication is live (Microsoft/Azure AD via Supabase, restricted to `@turbineenergyuk.co.uk` both client-side and via RLS — see Section 5 and migrations 003/004), but see the GitHub Pages note in Section 1 above before going live.

**What's still blocking the pilot from being fully populated and truly usable end-to-end:**
1. **Solar enrichment hasn't run yet** — both API keys arrived from Turbine IT 2026-08-17 and are wired in (`GOOGLE_SOLAR_API_KEY` set as a Supabase secret; Maps key still to be embedded, see item 3 below), but **billing is not enabled on the Google Cloud project** (project #380039802064) — every real API call currently fails with `PERMISSION_DENIED: This API method requires billing to be enabled`. This blocks both the Solar API and, almost certainly, the Maps JavaScript API (Google Maps Platform has required a billing account on every key, including free-tier usage, since 2018). **Get Turbine IT to enable billing on that project before invoking `solar-enrichment` again or testing the satellite toggle** — a wasted invocation while billing is off just burns real (non-refundable) calls against the self-imposed monthly cap in item 1a below for zero result.
   - **1a. Self-imposed usage cap added 2026-08-17** (migration `005_api_usage_tracking.sql` + `supabase/functions/solar-enrichment/index.ts`): the function now tracks its own call count in a `api_usage` table and refuses to call the Solar API past **9,500 requests/month**, independent of whatever quota is set in the Google Cloud console — a defense-in-depth measure, not a replacement for also capping the console quota. Note the arithmetic: there are **35,141 geocoded rows** (up from 21,265 since VOA seeding, 2026-08-20/21) to check against a **10,000/month free tier**, so a full first pass will span at least 4 calendar months by design, even once billing is enabled.
   - Until enrichment actually runs, every row's `solar_status` is `pending`, and since the map defaults to showing only `prospect` rows, **the map will currently appear empty** even though the data is loaded.
2. **Azure App Registration received, wired into Supabase, and fully verified live — done 2026-08-17.** Configured under Authentication → Providers → Azure (Tenant ID `0750b9d3-513f-4030-8191-2d825d3c58f2`, discovered via Microsoft's public OpenID discovery endpoint for `turbineenergyuk.co.uk` since IT's handoff only included the Client ID, Object ID, and secret, not the Tenant ID itself; the Supabase field is labeled "Azure Tenant URL" and expects the full `https://login.microsoftonline.com/<tenant-id>` form, not the bare GUID). One more fix was needed beyond the provider config: Authentication → **URL Configuration** still had its **Site URL** defaulted to `http://localhost:3000`, so even though the code sends a dynamic `redirectTo`, Supabase ignored it post-login and bounced every sign-in back to localhost regardless of which URL initiated it. Fixed by setting **Site URL** to `https://catchsit.github.io/turbine-solar-prospects/` and adding `https://catchsit.github.io/turbine-solar-prospects/**` to **Redirect URLs**. **A real `@turbineenergyuk.co.uk` account has now completed sign-in successfully end-to-end on the live URL** — the login gate is fully working, not just wired up.
3. **Google Maps JavaScript API key not yet embedded** — key received 2026-08-17, restricted to `https://catchsit.github.io/*` and Maps JavaScript API only, but the satellite-imagery toggle hasn't been rebuilt to use it yet (Section 6), and testing is blocked on the same billing issue as item 1. Non-blocking for launch — the map works fine without it.
4. **`COMPANIES_HOUSE_API_KEY` registered and verified live** — the `company-lookup` Edge Function (Section 5, Section 6) is fully built and deployed, and the free Companies House API key it needs (no billing required, unlike the Google keys — `developer.company-information.service.gov.uk`) is registered as a Supabase secret and has been exercised against real Companies House data. See Section 4 Step 0 and Section 6.

**Gotcha hit while wiring these up:** both API keys were originally pasted with a casing typo (`Aiza...` instead of the correct `AIza...` — every real Google API key starts `AIza`, capital I). Google's error messages for a malformed key ("API key not valid") look identical to a genuinely wrong key, so if a freshly-issued key gets rejected, check the casing before assuming IT sent a bad key.

---

## 2. Why This Exists (read before changing the filtering logic)

Two hard problems had to be solved before any of this could be built, and the design only makes sense in light of them:

1. **There is no public per-building electricity-consumption dataset in the UK.** Smart meter and DNO data isn't open. The proxy used here is the **non-domestic EPC register** (property type + floor area + energy rating) — it correlates with usage, it does not measure it. Say so in any UI copy; don't let "prospect" read as a guarantee.
2. **"Does this roof already have solar" needs aerial-imagery analysis.** After comparing a custom computer-vision build, licensing an existing UK solar-panel database (Geospatial Insight's LOCATE PV — enterprise sales process, unknown pricing), and Google's Solar API, this project uses the **Google Solar API**'s `buildingInsights` endpoint with `additionalInsights=DETECTED_ARRAYS`, which returns both roof solar potential and an existing-array detection status in one self-serve, pay-as-you-go call (10,000 free requests/month).

---

## 3. Tech Stack & Architecture

| Library | Purpose |
|---|---|
| Leaflet.js 1.9.4 + MarkerCluster 1.5.3 | Interactive map rendering |
| Plain HTML/CSS/JS | No framework, no build toolchain — same as mcs-map |
| Node.js scripts (`scripts/`) | EPC ingestion, geocoding — run manually, not in-browser |
| Supabase (Postgres) | Stores the enriched `prospects` table |
| Supabase Edge Function (Deno) | `solar-enrichment` — batched Google Solar API calls |
| Google Solar API | Roof solar potential + existing-array detection |
| postcodes.io | Free bulk UK postcode → lat/lng geocoding |
| GitHub Pages | Hosts the static `index.html` |

```
turbine-solar-prospects/
├── index.html                        # Prospect map, gated by Azure AD login — includes the Log Contact modal
├── dashboard.html                    # Manager-only CRM dashboard — activity charts, contact log, CSV export
├── HANDOVER.md                       # This file
├── package.json
├── .gitignore
├── shared/
│   ├── escape-html.js                # Copied verbatim from mcs-map
│   ├── solar-status-config.js        # solar_status -> {color, label}
│   ├── epc-rating-config.js          # EPC A-G -> {color, label}
│   ├── epc-recommendation-config.js  # epc_recommends_solar/efficiency -> {color, soft, label}
│   ├── sic-codes.js                  # UK SIC 2007 code -> description (Companies House sector text)
│   ├── contact-outcome-config.js     # prospect_contacts.outcome -> {color, soft, label, requiresDate}
│   ├── building-types.js             # BUILDING_TYPE_BUCKETS/bucketPropertyType/etc. — loads before talking-points.js
│   └── talking-points.js             # buildTalkingPoints() — client-side "why this building" summary
├── data/                             # gitignored — raw EPC CSV downloads go here
├── docs/superpowers/
│   ├── specs/2026-08-12-azure-ad-auth-design.md
│   ├── specs/2026-08-17-decision-maker-contact-design.md
│   ├── specs/2026-08-17-crm-contact-log-design.md
│   ├── specs/2026-08-19-companies-house-enrichment-design.md
│   ├── specs/2026-08-19-epc-recommendations-design.md
│   ├── specs/2026-08-19-voa-business-rates-design.md
│   ├── specs/2026-08-20-company-sector-maturity-classification-design.md
│   ├── specs/2026-08-20-popup-relevance-and-tabs-design.md
│   ├── specs/2026-08-20-voa-primary-prospect-source-design.md
│   ├── specs/2026-08-21-proposal-attachments-design.md
│   ├── specs/2026-08-21-solar-recommendation-recency-design.md
│   ├── plans/2026-08-12-azure-ad-auth.md
│   ├── plans/2026-08-17-crm-contact-log.md
│   ├── plans/2026-08-17-decision-maker-contact.md
│   ├── plans/2026-08-19-companies-house-enrichment.md
│   ├── plans/2026-08-19-epc-recommendations.md
│   ├── plans/2026-08-19-voa-business-rates.md
│   ├── plans/2026-08-20-company-sector-maturity-classification.md
│   ├── plans/2026-08-20-popup-relevance-and-tabs.md
│   ├── plans/2026-08-20-voa-primary-prospect-source.md
│   ├── plans/2026-08-21-proposal-attachments.md
│   └── plans/2026-08-21-solar-recommendation-recency.md
├── scripts/                          # Manually-run Node pipeline tooling
│   ├── ingest-epc.mjs                # CSV -> region+floor-area filter -> dedupe -> upsert `prospects`
│   ├── geocode-postcodes.mjs         # postcodes.io bulk lookup -> fills lat/lng; also backfills local_authority
│   ├── ingest-epc-recommendations.mjs # recommendations CSV -> LMK_KEY match -> epc_recommends_solar/efficiency flags (optional, additive) — run for real 2026-08-21
│   ├── ingest-business-rates.mjs     # self-downloads VOA compiled rating list (unzipper dep) -> parse -> filter -> upsert `business_rates_matches`; also seeds brand-new `source: 'voa'` prospects (Section 4, Step 6)
│   └── classify-companies.mjs        # bulk Companies House sector/maturity classification, rate-limited standalone script (not an edge function) — Section 4, Step 7
└── supabase/
    ├── config.toml                    # Minimal — pins verify_jwt=true for company-lookup only
    ├── migrations/
    │   ├── 001_prospects_schema.sql
    │   ├── 002_prospects_rls.sql
    │   ├── 003_prospects_auth_rls.sql  # Drops public read, requires authenticated (superseded by 004)
    │   ├── 004_prospects_domain_rls.sql # Narrows read further to @turbineenergyuk.co.uk (RLS-enforced)
    │   ├── 005_api_usage_tracking.sql  # api_usage table — Solar API monthly-cap tracking
    │   ├── 006_company_lookups.sql     # company_lookups table — Companies House cache
    │   ├── 007_prospect_contacts.sql   # prospect_contacts table — CRM contact log, manager-only read RLS
    │   ├── 008_prospect_contacts_insert_domain_check.sql # tightens 007's insert policy to @turbineenergyuk.co.uk + own identity, lowercases the admin-read check
    │   ├── 009_epc_recommendations.sql # epc_recommends_solar/epc_recommends_efficiency nullable boolean columns on `prospects`
    │   ├── 010_business_rates.sql      # business_rates_matches table (comment superseded by 011, see Section 5/7)
    │   ├── 011_business_rates_rls.sql  # business_rates_matches real SELECT policy — frontend reads this table directly
    │   ├── 012_company_classifications.sql # company_classifications table — sector/maturity data, real SELECT policy in the same migration
    │   ├── 013_voa_prospect_source.sql # epc_lmk_key made nullable, voa_ba_reference/source columns — lets VOA seed brand-new prospects, not just enrich
    │   ├── 014_prospect_proposals.sql  # prospect_proposals table + private `proposals` Storage bucket + their RLS policies
    │   └── 015_proposals_bucket_limits.sql # sets the `proposals` bucket's file_size_limit (20MB) / allowed_mime_types (PDF only) — server-side enforcement 014 alone left NULL
    └── functions/
        ├── solar-enrichment/
        │   └── index.ts               # Batched, resumable Google Solar API enrichment
        └── company-lookup/
            └── index.ts               # On-demand Companies House lookup per prospect
```

As of the Azure AD login work, `index.html` **does** talk to Supabase directly, matching mcs-map: the anon key is embedded client-side (safe — RLS is the real gate) and the frontend queries `prospects` live, behind a required Microsoft/Azure AD sign-in (`@turbineenergyuk.co.uk` only). See `docs/superpowers/specs/2026-08-12-azure-ad-auth-design.md` for the full design. The pipeline scripts (ingest, geocode, solar-enrichment) still write server-side using the service-role key, unaffected by this change.

---

## 4. Data Pipeline — how to (re)run it

Every step is idempotent (upserts on `epc_lmk_key`, `solar-enrichment` only touches `pending` rows), so re-running is always safe.

### Step 0 — one-time setup
1. Supabase project `turbine-solar-prospects` is already created. Confirm migrations `001_prospects_schema.sql` through `015_proposals_bucket_limits.sql` (all fifteen, in order) have been run in its SQL editor — run any that haven't (check with `SELECT * FROM prospects LIMIT 1;`; a "relation does not exist" error means `001`/`002` haven't been run yet). `004` is the one that actually enforces the `@turbineenergyuk.co.uk` restriction at the database level — don't treat `003` alone as sufficient, see Section 5. `005`/`006`/`007` add `api_usage`, `company_lookups`, and `prospect_contacts`, needed by `solar-enrichment`, `company-lookup`, and the Log Contact modal/dashboard respectively. `008` tightens `007`'s `prospect_contacts` insert policy to `@turbineenergyuk.co.uk` accounts inserting under their own identity — see Section 5. `009` adds the `epc_recommends_solar`/`epc_recommends_efficiency` columns needed by `scripts/ingest-epc-recommendations.mjs` (Step 5 below). `010` adds `business_rates_matches`, needed by `scripts/ingest-business-rates.mjs` (Step 6 below); `011` adds its real SELECT policy — don't treat `010` alone as sufficient, same lesson as `003`/`004`, see Section 5 and Section 7. `014` adds the `prospect_proposals` table and the private `proposals` Storage bucket for the Proposals popup tab's upload/reopen flow — see Section 5; `015` sets that bucket's `file_size_limit`/`allowed_mime_types` (20 MB, PDF only) so Storage enforces server-side what the client already checks — don't treat `014` alone as sufficient, same lesson again.
2. Register a GOV.UK One Login account (needed to download EPC bulk data — see Section 7).
3. Get a Google Cloud API key with the Solar API enabled, and set it as the `GOOGLE_SOLAR_API_KEY` secret on the Supabase project (`supabase secrets set GOOGLE_SOLAR_API_KEY=...`).
4. Register a free Companies House API key (`developer.company-information.service.gov.uk` — no billing/payment method required, unlike the Google keys) and set it as the `COMPANIES_HOUSE_API_KEY` secret (`supabase secrets set COMPANIES_HOUSE_API_KEY=...`). Done — see Section 1, item 4.
5. `npm install` in the repo root.

### Step 1 — download EPC data (manual, human-gated)
Go to https://get-energy-performance-data.communities.gov.uk/, sign in, download the **non-domestic certificates** bulk CSV per year (England & Wales) — this is the primary/required download, everything in Steps 2-4 below depends on it. Save into `data/` (gitignored).

The portal also offers a separate **recommendations** bulk CSV per year — a different dataset (the specific improvement measures an assessor recommended per certificate), now also used, optionally, by `scripts/ingest-epc-recommendations.mjs` (Step 5 below) to flag solar/efficiency recommendations. Not required for the core pipeline (Steps 1-4); download it only if you want those extra flags. Save into `data/recommendations/` (gitignored — see Section 7, risk 1).

`scripts/ingest-epc.mjs`'s `COLUMN_CANDIDATES` map has been verified against a real 2011–2026 export (Section 7, risk 2) — a first run shouldn't need any changes. If the portal changes its schema again in the future, the script still fails loudly and lists the actual headers found, rather than silently mis-mapping columns.

### Step 2 — ingest
```
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run ingest -- data/your-export.csv
```
Filters to Yorkshire & Humber (by local authority name, cross-checked against postcode outcode prefix) and floor area > `MIN_FLOOR_AREA_M2` (500, tune the constant at the top of the script), dedupes by UPRN keeping the most recent certificate, upserts into `prospects`.

### Step 3 — geocode
```
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run geocode
```
Fills `lat`/`lng` via postcodes.io's bulk endpoint for any row missing them.

### Step 4 — solar enrichment
Deploy and invoke the Edge Function repeatedly until no `pending` rows remain (each invocation processes a bounded batch, ~300 rows, to stay inside the function timeout):
```
supabase functions deploy solar-enrichment
supabase functions invoke solar-enrichment   # repeat until processed count is 0
```
Requires the `GOOGLE_SOLAR_API_KEY` secret set on the Supabase project. Watch the invocation logs for the `prospect / has_solar / no_coverage / error` funnel counts before this scales past the pilot — `buildingInsights` free tier is 10,000 requests/month.

There is no separate export step — `index.html` queries the `prospects` table live (Section 6), gated by the Azure AD login and RLS, so once Steps 1–4 have run, the data is already visible in the map on next load. (The old `npm run export` script that pushed a static `prospects.json` to GitHub was retired when live Supabase queries replaced it — see `docs/superpowers/specs/2026-08-12-azure-ad-auth-design.md`.)

### Step 5 — EPC recommendations enrichment (optional, additive — **run for real 2026-08-21**)
```
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run ingest-recommendations -- data/recommendations/your-export.csv
```
Reads the separate "recommendations" bulk CSV (Step 1 above — save it into `data/recommendations/`), matches rows to existing `prospects` by `LMK_KEY`/`certificate_number` (only prospects already in the table from Steps 1-2 are touched — a recommendations row for a certificate outside the Yorkshire & Humber/floor-area filter is skipped, and `source: 'voa'` prospects, which have no `epc_lmk_key` at all, are never touched by this step), and sets `epc_recommends_solar`/`epc_recommends_efficiency`. Entirely optional and additive — the map and CRM work fully without it. Prospects with no matching recommendations data stay `null` on both columns (not `false`) — see `docs/superpowers/specs/2026-08-19-epc-recommendations-design.md`. Full design in that spec and `docs/superpowers/plans/2026-08-19-epc-recommendations.md`.

**Status: run for real against the full 2011–2026 download, 2026-08-21 — 14,947 prospects flagged solar, 17,123 flagged efficiency.** The first real attempt classified **zero** rows: the column-name/text-matching logic had been guessed from domestic-EPC guidance text before ever inspecting a real non-domestic export, and both the guessed headers (`LMK_KEY`/`IMPROVEMENT_SUMMARY_TEXT`) and the text patterns were wrong — the real headers are `certificate_number`/`recommendation_code`/`recommendation`, and the real free text is terser than assumed (`"Consider installing PV."` contains neither "solar" nor "photovoltaic"). Fixed by re-verifying against all 16 real downloaded files: classification now keys primarily off `recommendation_code` (`EPC-R3` = solar water heating, `EPC-R4` = PV — confirmed stable across every year 2012–2026), with the original text patterns kept only as a fallback. Re-run afterward produced the real counts above. The remaining ~15,857 `null` rows are expected, not a bug — mostly the 15,118 `source: 'voa'` prospects (no `epc_lmk_key` to match on at all), plus a small number of genuinely clean EPC certificates with no recommendations listed.

### Step 6 — VOA business rates enrichment (optional, additive)
```
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run ingest-business-rates
```
Unlike every other source in this pipeline, this step is **fully automated — no manual download**: the script discovers the current VOA compiled non-domestic rating list itself (an unauthenticated Azure blob listing), downloads the ~93MB zip into `data/business-rates/` (gitignored), extracts only the non-historic entries file (~511MB uncompressed, streamed via `unzipper` — never buffered in memory), parses the asterisk-delimited positional CSV, filters to Yorkshire & Humber postcodes already present in `prospects`, and upserts one `business_rates_matches` row per matched prospect listing every hereditament (rated unit) at that postcode. Idempotent — safe to re-run; a re-run just re-upserts the same `prospect_id` rows. Entirely optional and additive — the map and CRM work fully without it. Full design in `docs/superpowers/specs/2026-08-19-voa-business-rates-design.md`.

**This step now also seeds brand-new prospects, not just enriches existing ones — built 2026-08-20, run for real 2026-08-20/21: 15,118 new `source: 'voa'` prospects seeded (37,134 total prospects now, up from 22,016).** EPC certificates only get lodged on a sale/lease/refurbishment, so a long-term owner-occupied building can be structurally invisible to the pipeline even if it's a great solar candidate. Since the VOA file is independent of EPC history, the same script now also creates a new `source: 'voa'` prospect row (one per postcode, highest-value hereditament as the representative address/description/rateable-value) for any Yorkshire & Humber postcode with **no** existing prospect, above `MIN_VOA_RATEABLE_VALUE` (currently £15,000 — calibrated against the real rateable-value distribution of prospects that already qualify via floor area, not guessed; see `docs/superpowers/specs/2026-08-20-voa-primary-prospect-source-design.md`). Migration `013` made `epc_lmk_key` nullable and added `voa_ba_reference`/`source` columns to support this. `npm run geocode` was re-run afterward — **35,141 of 37,134 (~95%) of all prospects now have lat/lng.**

**Always run `DRY_RUN=1` first:**
```
DRY_RUN=1 SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run ingest-business-rates
```
Reports the `New-prospect candidate postcodes...` count with **zero writes** — no prospects inserted, no `business_rates_matches` touched. Check that number looks reasonable (not zero, not absurdly larger than the current ~22,000 prospects) before running for real without `DRY_RUN=1`. **After a real run, also re-run `npm run geocode`** — new VOA-sourced prospects start with no `lat`/`lng` and won't appear on the map until geocoded; that same step now also backfills `local_authority` (from postcodes.io's `admin_district`, which VOA itself doesn't provide a readable name for).

Known, accepted limitation: dedup is postcode-level only, same precision already accepted for Companies House/VOA enrichment matching elsewhere in this pipeline. If EPC ingest (`npm run ingest`) is re-run *after* a VOA seeding pass adds a prospect at some postcode, and that postcode subsequently gets a real EPC certificate, the postcode would end up with two prospects (one `source: 'epc'`, one `source: 'voa'`) rather than being merged — not handled automatically.

### Step 7 — Company sector & maturity classification (optional, additive — **partially run, needs a re-run — see status below**)
```
COMPANIES_HOUSE_API_KEY=... SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run classify-companies
```
Unlike `solar-enrichment`, this is a **plain script, not an edge function** — Companies House has no monthly cap (only a 600-requests/5-min rate limit), so a full pass runs to completion in one long sitting instead of needing repeated manual invocations spread over months. `fetchPendingProspects()` queries **all** prospects with a postcode and no existing `company_classifications` row (not filtered by `source` — it naturally covers `voa` prospects too, once they exist in the table), so it doesn't need re-targeting as new prospects get added. For each: searches Companies House, ranks candidates by the same building-number address match `company-lookup` uses, fetches the top match's profile (SIC codes, incorporation date, accounts-filing status), and upserts one row. Idempotent — safe to re-run; only processes prospects still missing a row. **Expect roughly 6-8 hours** at ~2 calls/prospect paced under the rate limit — run it in the background, not something to wait on. Full design in `docs/superpowers/specs/2026-08-20-company-sector-maturity-classification-design.md`.

**Status: partially run, needs a re-run.** A first run on 2026-08-20 classified **18,933 of the 22,016 `source: 'epc'` prospects** before this session moved on to other work (the remaining ~3,083 hit transient `search failed` errors along the way and were skipped, safely retryable). Its pending-count is computed once at script start from whatever's in `prospects` at that moment — since it ran **before** the VOA-seeding pass (Step 6) added 15,118 more prospects, none of those VOA prospects have ever been classified (`0 of 15,118`). One more run will sweep up both the ~3,083 EPC leftovers and all 15,118 VOA prospects in a single pass (~18,200 total, same ~6-8 hour estimate) — see Section 9 "Next steps." Until then, "Business sector"/"Company maturity" sidebar filters and popup tags are populated for roughly half the map, not all of it. `accounts_type`'s field path (`json.accounts?.last_accounts?.type`) was left unverified against a real response during the first run — worth checking early in the next run (see `supabase/functions/company-lookup/index.ts`'s `fetchProfile()` and the matching note in `classify-companies.mjs`) before trusting it at scale.

Repeat steps 1–7 (or just 3–4 if only re-checking solar status) whenever the pilot needs refreshing — no cron is set up yet (see Section 8).

---

## 5. Database Schema

### `prospects` table
| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key (a real FK target, unlike mcs-map's soft `installer_id text` convention) |
| `epc_lmk_key` | text | Unique — EPC certificate key, upsert conflict target |
| `uprn` | text | Nullable — not all EPC rows carry one |
| `address`, `postcode`, `local_authority` | text | From EPC |
| `region` | text | Pilot tag, default `'yorkshire-humber'` |
| `property_type`, `total_floor_area` | text, numeric | From EPC — the usage proxy |
| `current_energy_rating`, `current_energy_efficiency` | text, int | EPC A–G band + score |
| `lodgement_date` | date | Used for dedupe (keep most recent per building) |
| `lat`, `lng`, `geocode_source` | numeric, numeric, text | Filled by `geocode-postcodes.mjs` |
| `solar_status` | text | `pending` \| `prospect` \| `has_solar` \| `no_coverage` \| `error` |
| `solar_checked_at`, `solar_detection_status`, `solar_max_panels`, `solar_yearly_energy_kwh`, `solar_raw` | — | Filled by `solar-enrichment`; `solar_raw` keeps the full API response so reclassification doesn't need a second paid call |
| `epc_recommends_solar`, `epc_recommends_efficiency` | boolean, nullable | Filled by `scripts/ingest-epc-recommendations.mjs` (migration `009`, optional/additive — Section 4 Step 5); `null` means no recommendations data found for that `LMK_KEY`, `true`/`false` means a match was found and did/didn't fall in that bucket — see `docs/superpowers/specs/2026-08-19-epc-recommendations-design.md` |

RLS: authenticated `SELECT` only, further restricted to `@turbineenergyuk.co.uk` accounts at the database level (migration `004_prospects_domain_rls.sql` supersedes `003_prospects_auth_rls.sql`'s "any authenticated session" policy — `003` alone was found in review to be bypassable by anyone who self-registers via the exposed anon key, since public email signup is enabled on the project; `004` closes that by checking `auth.jwt() ->> 'email'` in the policy itself). No client insert/update/delete policies — all writes are server-side via the service-role key.

### `prospect_contacts` table (migration `007`, insert/select policies superseded by `008`)

`id`, `prospect_id` (FK → `prospects.id`), `employee`, `employee_email`, `outcome`, `notes`, `next_action`, `follow_up_date`, `contacted_at` (defaults `now()`), plus mcs-map-matching `updated_at`/`updated_by`/`deleted_at`/`deleted_by` columns that nothing writes to yet (see Section 8). `outcome` has no DB-level `CHECK` constraint — enforced client-side only in the Log Contact modal, matching mcs-map's own contacts table exactly (see Section 6 for the 8-value enum).

**RLS is deliberately asymmetric, and worth reading carefully if you're comparing this to mcs-map's equivalent `contacts` table:**
- **Insert: `@turbineenergyuk.co.uk` accounts only, and only under their own identity — tightened by migration `008`, 2026-08-17 final review.** `007` originally shipped `to authenticated with check (true)` — any self-registered account (public email signup is enabled on this Supabase project) could insert arbitrary rows with a fake `employee` name, the same bug class already fixed twice elsewhere on this project (`003`→`004` for `prospects` reads, Section 5 below; and the `company-lookup` postcode/domain fix, Section 7 risk 9). `008` supersedes `007`'s insert policy (matching how `004` superseded `003` — an already-applied migration is never edited in place) with `lower(auth.jwt() ->> 'email') like '%@turbineenergyuk.co.uk' and employee_email = auth.jwt() ->> 'email'`, so a row's `employee_email` must match the inserting session's own JWT email — closing a related gap where the client previously supplied `employee_email` unchecked.
- **Select: manager-only.** `using (lower(auth.jwt() ->> 'email') = 'greg@turbineenergyuk.co.uk')` — nobody else, including the rep who wrote a given row, can read `prospect_contacts` back. This is a **deliberate deviation from `mcs-map`'s `contacts` table**, which allows any authenticated read. It was a conscious design decision (see "Non-goals" in `docs/superpowers/specs/2026-08-17-crm-contact-log-design.md`), not an oversight or a stricter-by-default accident — don't "fix" it to match mcs-map without checking with the client first, and don't assume the two sibling projects' CRM tables behave the same way. `008` also added `lower(...)` around the JWT-email comparison here (`007` compared without lowercasing) so a case-mismatched email fails loudly instead of silently rendering an empty dashboard.
- No update/delete policies at all in this iteration — the table is currently append-only from the client's perspective (Section 8).

Full rationale in `docs/superpowers/specs/2026-08-17-crm-contact-log-design.md`.

### `api_usage` table (migration `005`)
`(api_name, period)` → `request_count`, where `period` is a UTC `YYYY-MM` string. Written only by `solar-enrichment` via the service-role key (RLS enabled, no policies — no client can read or write it). Exists purely so the Edge Function can self-enforce the 9,500/month Solar API cap (Section 1, item 1a) without trusting the Google Cloud console quota alone.

### `company_lookups` table (migration `006`)
`prospect_id` (PK, FK → `prospects.id`) → `companies` (jsonb array of `{ company_name, company_number, status, officers: [{ name, role }], psc: [{ name, natures_of_control, is_corporate }], sic_codes: string[], incorporated_on: string | null }`), `no_match`, `fetched_at`, `source`. Written only by the `company-lookup` Edge Function via the service-role key (RLS enabled, no policies). Caches Companies House results per prospect for 90 days so re-opening the same popup doesn't re-hit the API; `no_match` distinguishes "checked, nothing there" from "never checked" so a genuine no-match doesn't get needlessly re-queried. Full design in `docs/superpowers/specs/2026-08-17-decision-maker-contact-design.md`.

### `business_rates_matches` table (migrations `010`/`011`)

`prospect_id` (PK, FK → `prospects.id`) → `hereditaments` (jsonb array of `{ description, rateable_value, billing_authority_code }`, one entry per rated unit VOA lists at that postcode), `no_match`, `fetched_at`. Written by `scripts/ingest-business-rates.mjs` via the service-role key (Section 4, Step 6). Same three-state absent/`no_match`/matched convention as `company_lookups`: a prospect with no postcode (or one VOA simply has no compiled-list entry for) gets **no row at all**, not a `no_match: true` row — `no_match: true` specifically means "this postcode was checked against the VOA list and nothing matched," not "never checked." Don't conflate the two when reading the table.

**Unlike `company_lookups`/`api_usage`, this table has a real SELECT policy** — migration `011`, added after `010` shipped with the "no policies, service-role only" pattern copied from those two tables. That pattern is wrong here: `index.html` reads `business_rates_matches` directly from the frontend's own `window.db` client (the signed-in user's `authenticated`-role session), not via a server-side Edge Function, so a service-role-only table is unreachable from the browser — PostgREST doesn't error in that case, it silently returns `null`/empty, which is exactly what shipped and went unnoticed until review. `011` mirrors `004`'s domain-restricted read policy (`@turbineenergyuk.co.uk` only). See Section 7 for the generalized lesson.

### `prospect_proposals` table (migration `014`, bucket limits `015`)

`id`, `prospect_id` (FK → `prospects.id`), `file_name`, `storage_path` (key into the private `proposals` Storage bucket), `file_size_bytes`, `uploaded_by`, `uploaded_by_email`, `uploaded_at` (defaults `now()`). Written by `index.html`'s `uploadProposal()` directly from the signed-in rep's own `authenticated` session (no Edge Function involved), read by `loadProposals()` for the popup's Proposals tab. No update/delete policies — append-only from the client's perspective, same convention as `prospect_contacts`.

**Its SELECT policy is deliberately wider than `prospect_contacts`' manager-only read (Section 5 above)** — any `@turbineenergyuk.co.uk` account can read `prospect_proposals`, not just `greg@turbineenergyuk.co.uk`, so a rep can reopen a proposal they (or a colleague on the same lead) previously sent. This is a new, separate policy on a new table; `prospect_contacts` itself is untouched and still manager-only. Insert is domain-restricted and identity-checked the same way `prospect_contacts`' `008` insert policy is (`uploaded_by_email` must match the caller's own JWT email). The `proposals` Storage bucket carries matching `authenticated` + domain-restricted insert/select policies for the PDF bytes themselves; `015` additionally sets the bucket's `file_size_limit` (20 MB) and `allowed_mime_types` (`application/pdf` only) so Storage enforces server-side what the client already checks in `uploadProposal()` — `014` alone left both `NULL`, so the API would silently accept any size/type from a caller that bypassed the UI.

**Known, accepted gap, not a bug to chase:** if a Storage upload succeeds but the following `prospect_proposals` insert fails (RLS rejection, network drop, etc.), the uploaded file is orphaned in Storage with no DB row and no UI ever surfaces it. There's no rollback and no delete policy — append-only is intentional here, same as `prospect_contacts` — so this was a deliberate trade-off, not something to build cleanup for without a specific reason to.

---

## 6. Frontend (`index.html`)

Gated by a Microsoft/Azure AD login screen (`@turbineenergyuk.co.uk` only) — see Section 1 and `docs/superpowers/specs/2026-08-12-azure-ad-auth-design.md`. Once signed in, loads prospect data via a live, paginated Supabase query (`fetchAllProspects()` in `index.html`), not a static file.

- Sidebar filters: search (address/postcode), floor-area min/max, rateable-value min/max (£, VOA business rates — see below), building-type chips, EPC rating chips (A–G, using the standard UK EPC colour band, kept distinct from the app's own Turbine Energy brand palette), solar-status chips.
- **Solar-status defaults to showing only `prospect`** — that's the point of the tool. A "show all" link reveals `has_solar`/`no_coverage`/etc. for spot-checking.
- Marker pin colour = `solar_status` (via `shared/solar-status-config.js`), following mcs-map's `makeMarkerIcon`/teardrop-pin pattern.
- Popup shows address, floor area, property type, local authority, EPC rating, solar status, and (for prospects with data) an estimated panel count / yearly kWh potential pulled from the Solar API response.
- **Rateable value (£) filter** — a min/max range filter, mirroring the floor-area filter's UI exactly. It compares against the **single highest-value hereditament** at a prospect's postcode (`maxHereditamentValue()`/`d._maxRateable` in `index.html`), not the sum and not "any hereditament in range" — see Section 7 risk 13 and `docs/superpowers/specs/2026-08-19-voa-business-rates-design.md`. The popup's **"Business rates at this postcode"** section (`businessRatesHtml()`) lists every hereditament VOA has at that postcode separately (same postcode-level-not-building-level caveat as the "Companies matched to postcode" section directly below it), sourced from `business_rates_matches` (Section 5), embedded in the same live `fetchAllProspects()` query — no separate popup-open network call, unlike Companies House.
- `BUILDING_TYPE_BUCKETS` (inline in `index.html`) groups EPC's `property_type` (UK planning Use Classes Order labels, not free text) into ~6 buckets via word-boundary keyword matching — spot-checked against real ingested data, see Section 7 risk 3 for the one known ambiguous case.

No radius circle (no obvious Turbine Energy depot location yet — ask the client).

**Log Contact modal.** Every prospect popup now has a "Log contact" button (`buildPopup()`) that opens a modal ported from mcs-map's `#modal-overlay`/`#modal` almost verbatim (same CSS classes, same Esc-to-cancel / ⌘/Ctrl+Enter-to-save shortcuts). Fields: outcome (chip picker + hidden `<select>`), optional notes, optional next action, and a conditionally-required follow-up date. The outcome enum has **8 values** — `No Answer`, `Follow Up`, `Meeting Booked`, `Survey Booked`, `Quote Sent`, `Converted`, `Not Interested`, `Already Has Solar` — colours, labels, and which ones require a date all live in one place, `shared/contact-outcome-config.js` (`CONTACT_OUTCOME`/`CONTACT_OUTCOME_ORDER`), rather than being duplicated between the modal and the dashboard. **3 of the 8 outcomes require a date** — `Follow Up`, `Meeting Booked`, `Survey Booked` — all sharing the one `follow_up_date` column (it's "the next important date tied to this contact," not three separate concepts); submission is blocked client-side with an alert if one of those three is picked and no date is set. `employee`/`employee_email` are pre-filled from the signed-in session (`getUserName(currentUser)`, `currentUser.email`), not typed in by the rep. Reps get no read-back of past contacts anywhere in the popup or modal — by design, see Section 5's RLS note and the design spec's Non-goals. Full design in `docs/superpowers/specs/2026-08-17-crm-contact-log-design.md`.

**`dashboard.html` — manager-only CRM dashboard.** A new top-level page, structurally mirroring mcs-map's `dashboard.html`: a daily-activity chart, a by-employee chart, a filterable/sortable contact log table, and CSV export. No "pipeline composition" chart (that visualizes mcs-map's own installer-status field, which has no equivalent here). Access is gated **two ways**, matching this project's established defense-in-depth pattern (compare the Azure tenant restriction + client-side domain check in Section 1/5):
- **Real gate:** RLS itself (Section 5) — a non-admin session's query against `prospect_contacts` returns zero rows no matter what the page tries to render.
- **UX gate:** the page also checks `isAdmin(currentUser)` on load (same `ADMIN_EMAILS`/`isAdmin()` helper as `index.html`, now finally wired up — see Section 7 risk 8) and shows a `#denied-state` panel ("This page is restricted to managers.") in place of the dashboard, so a rep who finds the URL doesn't land on a broken, empty-looking page and wonder if something's wrong. It does not redirect back to `index.html` — the denied message is rendered directly on `dashboard.html` itself.

Three limitations flagged in the original review (period switcher not reaching the table/export, missing soft-delete filter, no CSV formula-injection guard) were fixed in a 2026-08-17 final-review pass — see Section 7, risks 10–12. That pass also added a domain-restricted INSERT policy (migration `008`) closing the same "any authenticated session" gap already fixed twice elsewhere on this project (Section 5), fixed `dashboard.html`'s pagination and sort, added the `index.html` nav link that finally wires up `isAdmin()` in that file (risk 8), made the Log Contact modal's employee field genuinely read-only, and grouped the by-employee chart/filter by the stable `employee_email` instead of the free-text name.

**Satellite imagery — in progress, not yet live.** Three free/no-key options were tried and rejected on 2026-08-12 by comparing real tiles over the same Leeds location: Esri World Imagery (too low-resolution to judge a rooftop) and MapTiler (added, then also reverted — see commits `9b389e1` → `019edc2` → `0cd1196`). The map currently reverted to plain OpenStreetMap streets only, no toggle. Google Maps satellite tiles looked the best of the options compared (Bing, Google, MapTiler, Esri) but need the Google Maps JavaScript SDK bridged into Leaflet rather than a simple tile-URL swap, and a browser-exposed, domain-restricted API key. That key was requested from Turbine IT on 2026-08-17: **Application restrictions → Websites → `https://catchsit.github.io/*`** only (no localhost entry — it wasn't accepted in the console, so testing is being done directly against the live Pages URL instead of locally), **API restrictions → Maps JavaScript API** only. Once the key arrives, wire up the Leaflet-Google bridge and re-add the toggle — do not reuse the Solar API key, which is a separate, server-side-only credential (Section 1).

**Company match + Talking points — built and deployed, not yet live-testable.** Opening a prospect's popup now shows two new sections, added 2026-08-17 (full design in `docs/superpowers/specs/2026-08-17-decision-maker-contact-design.md`, don't duplicate it here):
- **Talking points** — a client-side-only "why this building" summary generated by `shared/talking-points.js` from data already on the row (EPC rating/efficiency, floor area + building-type bucket, and — once solar enrichment has run — real `solar_max_panels`/`solar_yearly_energy_kwh`, no invented £ figures). No network call, always renders.
- **Company match** — fired automatically (fire-and-forget, its own loading state) via a new `company-lookup` Edge Function that looks up Companies House for a plausible company + current directors registered at the prospect's postcode, caching the result in the new `company_lookups` table (Section 5) for 90 days. On no match, no postcode, or any lookup failure, it falls back to one-click manual search links (Google, Companies House, LinkedIn) rather than a dead end — the popup never blocks or breaks on this.

Requires the `COMPANIES_HOUSE_API_KEY` secret (free — `developer.company-information.service.gov.uk`, no billing needed, unlike the Google keys), which **is registered and verified live** (see Section 1, item 4). The function is fully built and deployed; live browser testing on 2026-08-17 confirmed it gracefully degrades to the manual-search fallback when the key is absent (now via a clean `503`, not a crashed worker — see Section 7, risk 9), and real Companies House matching has since been exercised against the live key.

**Building-relevance ranking + tabbed popup layout — built and deployed 2026-08-20, not yet live-verified.** Full design in `docs/superpowers/specs/2026-08-20-popup-relevance-and-tabs-design.md`. Two changes, shipped together since the second gives the first a home:
- `company-lookup` now ranks postcode-matched companies so ones plausibly *in the clicked building* (a leading-number match between the prospect's address and the company's `premises`/`address_line_1`) sort first — never hides a company, only reorders, and fixes a real bug where the old 5-item cap applied before any relevance signal existed. Cap raised to 8 now that lower-relevance matches are visually de-prioritized rather than competing for a scarce slot. Matched companies get a "Likely this building" badge in the popup.
- The popup itself is now tabbed (`Business rates` / `Companies`, Business rates active by default) instead of an unconditional vertical stack, and the floor area/property type/local authority fact lines consolidate into one line — meaningfully shorter popup, less scrolling.
- Verified via direct browser execution of `buildPopup()`/`businessRatesHtml()`/`switchPopupTab()` against fixture data (bypassing the Azure AD gate, which this environment has no real session for) and a disposable Node script sanity-checking the ranking logic — **not yet exercised against real Companies House data through a live signed-in session.** See the live-verification checklist in `docs/superpowers/plans/2026-08-20-popup-relevance-and-tabs.md`, Task 3, Step 3.

**Next step worth flagging:** `company-lookup` currently queries Companies House's `GET /search/companies?q=<postcode>` — a name-oriented free-text search that happens to index registered-office addresses, not a location-first endpoint. `GET /advanced-search/companies?location=<postcode>` may be a better-fit endpoint for this use case. Worth evaluating during the already-planned 10-15 prospect spot-check (design spec's "Testing & verification" section), now that `COMPANIES_HOUSE_API_KEY` is registered and live testing is possible.

**Recommendation date shown in the popup — built and deployed 2026-08-21.** `lodgement_date` (the EPC assessment date — effectively "when this solar/efficiency recommendation was made") is now selected by the frontend and shown inline on the existing "EPC recommends solar"/"EPC recommends efficiency improvements" tags (e.g. "EPC recommends solar · Mar 2019"), via a small `formatMonthYear()` helper. Purely additive — no schema change, since `lodgement_date` already existed on `prospects`. Gives reps a sense of how stale a recommendation is before contacting.

**Solar recommendation recency visual — built, reviewed, and deployed 2026-08-21.** Full design in `docs/superpowers/specs/2026-08-21-solar-recommendation-recency-design.md`. Two parts:
- Prospects where `epc_recommends_solar === true` now render with their marker color **replaced** by a green(newest)→red(oldest) gradient (`recencyColor()`/`interpolateColor()` in `index.html`), scaled dynamically to the real spread of `lodgement_date` values actually present among solar-recommended prospects — never a fixed calendar range. Every other prospect keeps its ordinary `solar_status` color, untouched. The gradient's exact colors (`RECENCY_COLOR_NEW`/`RECENCY_COLOR_OLD`) were tuned during final review to be visually distinct from the existing `SOLAR_STATUS.prospect`/`SOLAR_STATUS.error` palette (the first choice, `#22c55e`/`#c0392b`, was too close to both — the freshest leads looked like ordinary prospect pins).
- A new sidebar "Solar recommendation year" chip filter (`buildRecommendationYearChips()`), data-driven (one chip per year actually present, not a hardcoded range), following the exact "strict once touched" pattern as every other chip filter in this file. Each chip's dot is colored by its own position on the same gradient the markers use, doubling as a free legend.

**Proposal file attachments — built, reviewed, and deployed 2026-08-21.** Full design in `docs/superpowers/specs/2026-08-21-proposal-attachments-design.md`. A third popup tab, "Proposals" (alongside Business rates/Companies), lets any `@turbineenergyuk.co.uk` rep attach a PDF proposal (generated separately by the unrelated `turbine-proposals` desktop app, `C:\Users\GregRoy\turbine-proposals`) to a prospect and reopen it later — a standalone action, not tied to any `prospect_contacts` outcome. `loadProposals()` renders the list on popup open (same fire-and-forget pattern as `loadCompanyMatch()`); `uploadProposal()` validates PDF-only/20MB-max client-side before ever hitting the network, then uploads to the private `proposals` Storage bucket followed by a `prospect_proposals` insert; `openProposal()` opens a 5-minute signed URL in a new tab (opened synchronously before the async signed-URL call, specifically to avoid Safari's popup blocker silently eating a post-`await` `window.open`). See Section 5 for the table/bucket/RLS design. **No proposals uploaded live yet (`prospect_proposals` has 0 rows as of 2026-08-21)** — built and reviewed but not yet exercised by a real rep.

**Prospect data loading — reliability prioritized over speed, 2026-08-21, after repeated live failures.** `fetchAllProspects()` had a rough day: growing to 37,134 rows (up from ~22,000) made the original one-page-at-a-time load meaningfully slow, which led to several same-day attempts to fix it, in order:
1. Parallelized the original offset-based (`.range()`) pagination — fixed nothing real; offset pagination itself was the problem (see #3).
2. `applyFilters()` was separately found to be adding matching markers to the Leaflet.markercluster group **one at a time** (`cluster.addLayer(m)` in a loop) — this recalculates the cluster's spatial index on every single insertion, and was likely the single biggest cost at this scale. Fixed to a single bulk `cluster.addLayers(toShow)` call instead. **This fix is solid and unrelated to the fetch issues below** — no further changes needed here.
3. The live site then started failing outright with Postgres `57014` ("canceling statement due to statement timeout") errors. Root cause, confirmed via `EXPLAIN ANALYZE` against the real live data: `.range(from, to)` compiles to SQL `OFFSET`, which Postgres can only satisfy by computing the full join across `prospects` × `business_rates_matches` × `company_classifications` (tens of thousands of rows each) and discarding the skipped rows — a deep page took **5.9 seconds** even with zero concurrency. Switched to keyset (cursor) pagination (`id > lastId ORDER BY id LIMIT 1000`), which lets Postgres seek via the primary-key index instead — the same deep page dropped to **13ms**.
4. Individual keyset pages still cost ~0.5-7s end-to-end in a real browser (network/serialization overhead well above the 13ms DB-side cost), so pages were then fetched concurrently (a small worker pool, later a two-phase id-list-then-bounded-range design). **Both concurrent variants caused live statement-timeout failures again** — every individual keyset query is fast in isolation, but something about running several of them concurrently against this specific table set pushes past `authenticated`'s 8-second `statement_timeout`. Not fully root-caused (a direct concurrency test against the live DB was attempted but the test method itself broke against Supabase's connection layer before yielding a conclusive answer).
5. Separately, the id-list precompute step introduced in one of the concurrent variants had its own bug: a plain `.select('id')` with no `.range()` silently hit PostgREST's default 1,000-row cap, so the whole fetch believed there was only one page — briefly making the live map show only 1,000 of 37,134 prospects. Fixed alongside the pagination itself, but worth remembering as its own lesson (Section 7 already has three prior instances of "PostgREST silently truncates/returns nothing" — this is a fourth-ish, slightly different flavor of the same family of gotcha).

**Current state, deliberately conservative: sequential keyset pagination, no concurrency at all.** `fetchAllProspects()` fetches a `head:true` count for the progress denominator, then loops one `id > lastId LIMIT 1000` page at a time, updating a live "(fetched / total) " count on the loading screen via `onProgress`. Slower (~90s for a full cold load) but has run error-free every time it's been tested this way. **Revisit concurrency only with a safer mechanism if the ~90s load becomes a real problem** — e.g. a server-side view/materialized cache that avoids the live multi-table join entirely, rather than more client-side request-shaping tricks, which have now caused three separate live incidents in one day.

---

## 7. Known Risks / Open Items

1. **EPC portal — resolved.** `epc.opendatacommunities.org` redirects to `get-energy-performance-data.communities.gov.uk`, which requires a GOV.UK One Login account. Confirmed via a real walkthrough: the portal offers separate **certificates** and **recommendations** downloads per year — **certificates** is the required download for the core pipeline (Section 4, Steps 1–4); **recommendations** (retrofit-suggestion data per certificate) is now also used, optionally, by `scripts/ingest-epc-recommendations.mjs` (Section 4, Step 5) to set the `epc_recommends_solar`/`epc_recommends_efficiency` flags — see `docs/superpowers/specs/2026-08-19-epc-recommendations-design.md`. Certificates are available per-year back to 2011; header schema is identical across all years 2011–2026.
2. **EPC CSV column names — verified against a real export, and fixed.** `scripts/ingest-epc.mjs`'s `COLUMN_CANDIDATES` map was a guess based on the historical (`opendatacommunities`) schema; the real bulk export uses different names for two fields the script needs: `LMK_KEY` → `certificate_number`, and `TOTAL_FLOOR_AREA` → `floor_area`. Both are now in `COLUMN_CANDIDATES` alongside the original guesses, and a full 2011–2026 ingest (1,059,502 raw rows) ran clean. The script still fails loudly and lists real headers if a future export changes again.
3. **`BUILDING_TYPE_BUCKETS` bucketing — spot-checked against real data, works for observed categories.** Real `property_type` values follow the UK planning Use Classes Order format (e.g. `"A1/A2 Retail and Financial/Professional services"`, `"B1 Offices and Workshop businesses"`, `"B2 to B7 General Industrial and Special Industrial Groups"`, `"B8 Storage or Distribution"`, `"C2 Residential Institutions - Hospitals and Care Homes"`), not free-text descriptions. The keyword matching correctly buckets all of these seen so far. One ambiguous case worth knowing: `"B1 Offices and Workshop businesses"` matches Warehouse/Industrial (via `"workshop"`) rather than Office, because Warehouse/Industrial is checked first and B1 is a genuinely mixed-use category — this is an order-dependent judgment call, not a bug, but worth revisiting if the sales team finds B1 buildings miscategorized. C1 (Hotels) and D1/D2 (Institutions/Assembly and Leisure) categories haven't been directly observed yet.
4. **Google Solar API `detectionStatus` field path is unverified.** `supabase/functions/solar-enrichment/index.ts`'s `classifyDetection()` checks a few plausible JSON paths and always stores the raw response in `solar_raw` specifically so this can be corrected by reprocessing stored data, without a second paid API call, once a real response is seen. **Do this check early in the pilot**, before trusting the `prospect`/`has_solar` split at any scale.
5. **Google Solar API coverage won't be uniform** across Yorkshire & Humber — expect a real `no_coverage` rate, especially for large sheds/industrial buildings on urban outskirts.
6. **EPC data is a proxy, not a measurement.** Self-declared at assessment time, buildings get renovated afterward. Keep the UI caveat in `index.html`'s footer.
7. **postcodes.io has no formal SLA.** Fine for a pilot; switch `scripts/geocode-postcodes.mjs` to a local ONSPD CSV join before any national-scale expansion — both for reliability and to avoid overloading a free public service.
8. **Auth is now live** (Azure AD via Supabase, matching mcs-map) — see `docs/superpowers/specs/2026-08-12-azure-ad-auth-design.md`. The `ADMIN_EMAILS`/`isAdmin()` stub in `index.html` is **now genuinely wired up, resolved 2026-08-17.** As of the CRM contact log work (migration `007`) it only gated `dashboard.html`'s own copy of the helper — `index.html`'s own `isAdmin()` was defined but never actually called anywhere in that file. The final-review pass on that work fixed this for real: `onAuthenticated()` in `index.html` now calls `isAdmin(user)` to show/hide an admin-only "Dashboard" link in the top nav (`#nav-dashboard-link`), so `index.html` itself now has a first real use of the helper, not just `dashboard.html`'s independent copy.
9. **`company-lookup` postcode/domain security fix, 2026-08-17 review.** The function originally accepted `{ prospect_id, postcode }` from the client and used the client-supplied `postcode` directly in the Companies House search — an authenticated-but-undomain-checked proxy for arbitrary Companies House searches (same bug class as `003` vs `004`'s RLS history in Section 5: "any authenticated session" isn't a real access boundary on this project, since public email signup is enabled on the Supabase project). Fixed: the request contract is now `{ prospect_id }` only, the postcode is looked up server-side from `prospects` using the service-role key, and the function independently verifies the caller's own JWT (via a second, anon-key Supabase client scoped to the request's `Authorization` header) rejects with `403 Forbidden` unless the email ends `@turbineenergyuk.co.uk` — mirroring `index.html`'s `onAuthenticated()`. `supabase/config.toml` now also pins `verify_jwt = true` on this function explicitly. The previous risk noted here (the module-level `throw` on a missing `COMPANIES_HOUSE_API_KEY` crashing the whole Deno worker before `Deno.serve` registered a handler, so even CORS `OPTIONS` preflight got a misleading 500) is also fixed — `COMPANIES_HOUSE_API_KEY` is no longer asserted at module load; a missing key now returns a clean `503` JSON error instead.
10. **`dashboard.html`'s period switcher only affects the two charts — resolved 2026-08-17 (final review).** Switching between "7 days" / "30 days" / "All time" (`periodData()`) used to re-slice only the daily-activity and by-employee charts while the contact-log table and CSV export always rendered the full, unfiltered `prospect_contacts` result set. Fixed: `filteredRows()` now starts from `periodData()` instead of `allData`, and the period button click handler also calls `renderTable()`, so the table and export both now match whichever period is active. `periodData()`'s cutoff was also changed to align to a calendar-day boundary (`cutoff.setHours(0,0,0,0)`), matching the daily-activity chart's calendar-date bucketing exactly, so the two charts sum to the same total for a given period.
11. **`dashboard.html`'s query doesn't filter out soft-deleted rows — resolved 2026-08-17 (final review).** `prospect_contacts` carries `deleted_at`/`deleted_by` columns (Section 5) so a future delete feature can soft-delete without a migration; there is still no delete UI (Section 8), so this was moot in practice, but the dashboard's `prospect_contacts` query now includes `.is('deleted_at', null)` as cheap insurance ahead of that future feature landing.
12. **CSV export has no formula-injection sanitization — resolved 2026-08-17 (final review).** `dashboard.html`'s export used to build rows straight from free-text fields (notes, employee name) with only quote-escaping, no check for a leading `=`, `+`, `-`, or `@`. Fixed: a `csvCell()` helper now prefixes any cell whose text starts with `=`, `+`, `-`, `@`, a tab, or a carriage return with a leading `'` before quoting, so it can't be interpreted as a formula when opened in Excel/Google Sheets — this matters for ordinary note-taking (e.g. "- called, no answer"), not just adversarial input. The export's `Prospect ID` column (a raw UUID) was also replaced with a human-readable `Prospect` column (the address/postcode, from the same `prospectNames` map the on-screen table uses).
13. **VOA business rates — one dated constant, and this project's third occurrence of the same RLS bug class, 2026-08-19 final review.**
    - `scripts/ingest-business-rates.mjs`'s `LIST_YEAR = '2026'` constant needs updating when VOA compiles its next rating list — **1 April 2029**. The script discovers the current epoch automatically within a list year, but not the list year itself; if the script starts failing to find a baseline zip after that date, this is the first thing to check.
    - **The RLS gap fixed by migration `011` (Section 5) is the third time this exact bug class has hit this project**: `003`→`004` for `prospects` reads, `007`→`008` for `prospect_contacts` inserts, now `010`→`011` for `business_rates_matches` reads. The reusable rule, worth applying automatically from now on rather than re-discovering: **before copying the "no policies — service-role only" pattern from `company_lookups`/`api_usage` onto a new table, check whether the table will actually be read directly by the frontend's own authenticated session** (as opposed to only via a server-side Edge Function or pipeline script using the service-role key) — if so, it needs a real SELECT policy. This is easy to ship without noticing because **PostgREST fails silently** when a table has RLS enabled and no matching policy: the query returns an empty/`null` result, not an error, so nothing looks broken locally until someone checks the actual data.

---

## 8. Not Yet Built (future work)

- Automated refresh (pg_cron) — v1 is a manually-run pipeline. EPC re-ingestion should be at most monthly once the portal's automation story is confirmed; solar re-checks should be far less frequent (6–12 months, and only for `has_solar` rows, to catch removed panels) since Google's own aerial imagery doesn't refresh often.
- Region expansion beyond Yorkshire & Humber — the `region` column and `scripts/ingest-epc.mjs`'s local-authority filter are the two places to widen.
- **Edit/soft-delete UI for logged contacts** — `prospect_contacts` already carries mcs-map-matching `updated_at`/`updated_by`/`deleted_at`/`deleted_by` columns (migration `007`, Section 5) precisely so this can be added later without a schema change, but no update/delete RLS policies or UI exist yet — the table is append-only from the client's perspective for now. Explicitly out of scope for the CRM contact log work, not an oversight — see "Non-goals" in `docs/superpowers/specs/2026-08-17-crm-contact-log-design.md`. Remember Section 7 risk 11 (dashboard doesn't filter `deleted_at`) the moment this lands.
- **Rep-facing read access to the contact log** — reps can only insert into `prospect_contacts`, never read it back (Section 5); the dashboard is manager-only. Also explicitly out of scope per the design spec's Non-goals — widening the RLS read policy would need a real decision, not a silent default. This is also why there's no duplicate-contact collision warning ("Sarah already called this prospect") — that would need rep-facing read access that isn't wanted right now.
- **Companies House "run check" trigger-mechanism change + its own activity log** — `company-lookup` (Section 6) still auto-fires on popup open rather than behind an explicit "Run check" button, and has no per-check activity log (who ran a check, on which prospect, when). This was raised during the CRM contact log brainstorming and deliberately deferred as a separate fast-follow, not part of what this task built — see "Deferred" in `docs/superpowers/specs/2026-08-17-crm-contact-log-design.md`. The same manager-only-RLS-plus-dashboard infrastructure built for `prospect_contacts` is now in place to extend for this.
- AI research agent for prospects Companies House can't resolve (sole traders, stale registered addresses) — designed conceptually alongside the Company match feature (Section 6) but deferred; would call Claude's Messages API with web search, restricted to the company's own site / Companies House / general search (never LinkedIn scraping), every claim carrying a source URL and an "AI-assisted — verify before use" label. **Blocked on** an Anthropic Console API key with billing, not available until the project owner has billing access at Turbine Energy. Full design in `docs/superpowers/specs/2026-08-17-decision-maker-contact-design.md`.

---

## 9. How to Continue Development

**Already done:**
- Repo created and pushed to `main` (https://github.com/CatchSit/turbine-solar-prospects); Supabase project `turbine-solar-prospects` created, migrations 001–015 applied. **Live prospect count is now 37,134** (22,016 `source: 'epc'` + 15,118 `source: 'voa'`) — treat the live count as authoritative over any older figure quoted elsewhere in this doc.
- `npm install`, GOV.UK One Login registered, full 2011–2026 non-domestic EPC bulk certificates downloaded and ingested (Section 4, Steps 1–2).
- Geocoding run (Section 4, Step 3), including a re-run after VOA seeding — **35,141 of 37,134 (~95%) have lat/lng**; the rest failed to match in postcodes.io (expected background noise, Section 7 risk 7).
- Turbine Energy's real brand palette applied (pulled from the `turbine-homepage` marketing site build) and the `BUILDING_TYPE_BUCKETS` keyword matching spot-checked against the real ingested data (Section 7, risk 3).
- Azure AD login gate built: Microsoft/Azure AD sign-in via Supabase Auth, restricted to `@turbineenergyuk.co.uk` both client-side and via RLS (migrations 003–004, live-verified against the real project — see `docs/superpowers/specs/2026-08-12-azure-ad-auth-design.md`).
- CRM contact-logging layer (Log Contact modal + manager-only `dashboard.html`, migrations 007–008, Section 5/6).
- Companies House enrichment (`company-lookup` Edge Function, migration 006) shipped, then extended with PSC/SIC/incorporation-date data (Section 6) — key registered, live-verified against real Companies House data.
- EPC recommendations pipeline built (migration 009) and **run for real 2026-08-21, after fixing a real bug** (Section 4, Step 5 — the first attempt classified zero rows due to guessed-wrong column names/text patterns) — **14,947 prospects flagged solar-recommended, 17,123 flagged efficiency-recommended**, both shown with their recommendation date in the popup and (solar) as a marker-color gradient (see below).
- VOA business rates enrichment built AND run for real (migrations 010–011, `scripts/ingest-business-rates.mjs`) — fully automated download/parse, no manual step. **All 37,134 prospects have a `business_rates_matches` row** (matched or explicitly `no_match`).
- Popup relevance ranking + tabbed layout (2026-08-20, `docs/superpowers/specs/2026-08-20-popup-relevance-and-tabs-design.md`) — companies badged "Likely this building," popup restructured into Business Rates/Companies/Proposals tabs (the third added 2026-08-21, see below), whitespace tightened, card widened to 360px. Live-verified in a real browser against fixture data; real-session live checklist still pending (Section 6).
- Company sector & maturity classification built (migration 012, `company-lookup` extended with `accounts_type`, `scripts/classify-companies.mjs`, sidebar filters, popup tags/badges) — **partially run: 18,933 of 22,016 EPC prospects classified, 0 of 15,118 VOA prospects** (the run happened before VOA seeding added them — Section 4, Step 7 has the full explanation and the re-run command).
- VOA as a primary prospect source, not just enrichment (migration 013) — **run for real, 15,118 new prospects seeded** (Section 4, Step 6).
- Proposal file attachments built (migrations 014/015, `prospect_proposals` table + `proposals` Storage bucket with server-side size/type limits, new Proposals popup tab — Section 5/6) — any authenticated `@turbineenergyuk.co.uk` rep can upload and reopen a PDF proposal against a prospect. **Built and reviewed 2026-08-21** — no real proposals uploaded live yet, and real end-to-end live verification with a signed-in session is still a pending manual step (Task 5 of `docs/superpowers/plans/2026-08-21-proposal-attachments.md`).
- Solar recommendation recency visual built and deployed 2026-08-21 (Section 6) — green→red marker gradient for solar-recommended prospects + a new "Solar recommendation year" sidebar filter.
- Prospect-loading performance and reliability work, 2026-08-21 (Section 6 has the full story) — landed on a reliable sequential keyset-pagination fetch after three separate live incidents from faster-but-concurrent alternatives; also fixed a real Leaflet.markercluster perf bug (`addLayer` in a loop → bulk `addLayers`) that's unrelated to and unaffected by that saga.

**Next steps:**
1. **Get billing enabled on the Google Cloud project** (#380039802064) that both API keys belong to — this is the main blocker right now (Section 1, item 1). `GOOGLE_SOLAR_API_KEY` is already set as a Supabase secret and `solar-enrichment` is deployed with the 9,500/month self-cap (migration `005`); it's ready to run the moment billing is on. Don't invoke it again before then — a call while billing is off still counts against the self-imposed cap for a guaranteed failure.
2. Run solar enrichment (Section 4, Step 4) — repeat the invoke command until the processed count is 0 or the response reports `budgetExhausted: true` (expected partway through, given 35,141 geocoded rows vs the 9,500/month cap — see Section 1, item 1a). Spot-check ~15–20 known buildings (some with visible rooftop solar, some without) before trusting the `prospect`/`has_solar` funnel at scale, and verify `classifyDetection()`'s field-path guess against a real response early (Section 7, risk 4).
3. ~~Get the Azure App Registration back from Turbine Energy's IT team and wire it into Supabase~~ — **done and fully verified 2026-08-17** (Section 1, item 2), including a real `@turbineenergyuk.co.uk` account completing sign-in live. Also required fixing Supabase's Auth → URL Configuration (Site URL was still defaulted to `localhost:3000`).
4. Test at the live URL (`https://catchsit.github.io/turbine-solar-prospects/`) — exercise every filter against the real live data once Step 2 above has populated `solar_status` beyond `pending`.
5. ~~Confirm Azure AD works end-to-end before announcing the URL~~ — **done**. The remaining launch gate is purely the empty-map problem from Step 2 (solar enrichment hasn't run) — see Section 1.
6. Once billing is confirmed on, embed the Google Maps JavaScript API key (already received, restricted to `https://catchsit.github.io/*` and Maps JavaScript API only — Section 1 item 3, Section 6) and wire up the satellite-imagery toggle via a Leaflet-Google bridge. Test against the live Pages URL, not localhost (the key has no localhost origin). Non-blocking for launch — the map works without it.
7. **Re-run `npm run classify-companies`** (Section 4, Step 7) with `COMPANIES_HOUSE_API_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` set — will pick up both the ~3,083 leftover EPC prospects and all 15,118 VOA prospects in one pass (~18,200 total). Expect ~6-8 hours. Once it's made real progress, spot-check the "Business sector" sidebar filter's chip counts and a popup on a known multi-tenant postcode to confirm the buckets look sensible — adjust `shared/sic-sector-config.js` if not (pure client-side config, no re-run needed).
8. Live-verify the popup relevance/tabs work against a real signed-in session — the checklist is in `docs/superpowers/plans/2026-08-20-popup-relevance-and-tabs.md`, Task 3, Step 3. The Proposals tab (upload/reopen a real PDF, confirm cross-rep visibility) needs the same kind of live pass — checklist in `docs/superpowers/plans/2026-08-21-proposal-attachments.md`, Task 5.
9. If the ~90s cold-load time (Section 6) becomes a real complaint, investigate a server-side fix (e.g. a materialized view pre-joining `prospects`/`business_rates_matches`/`company_classifications`) rather than more client-side concurrency — that avenue has now caused three live incidents in one day and is not worth re-attempting without a different approach.
